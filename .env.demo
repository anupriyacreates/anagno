# Showcase / demo mode — loaded by the `:demo` npm scripts (vite --mode demo).
# With this on, the app returns canned data for every AI call: no backend,
# no API key, no network. Safe to commit (contains no secrets).
VITE_DEMO_MODE=1
